const fs = require('fs');
// Let's just use the clean CSS from the live site since the original one had a minor parsing issue.
// Actually, I can just grab the exact css file again and leave it alone, but format it.
